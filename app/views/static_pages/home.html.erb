<% if user_signed_in? %>
<!--
<aside class="col-md-4">
  <section class="user_info">
    <%= render 'shared/user_info' %>
  </section>
</aside>
-->
  <h1>My items (<span class="item_count"><%= @items.count %></span>)</h1>
  <section class="item_add">
    <%= render 'shared/item_form' %>
    <button type="button" class="btn btn-primary btn-md btn-block btn_add_item">Add New Item</button>
  </section>
  <% if @items.any? %>
  <section class="item_search">
    <%= render 'shared/item_search_form' %>
  </section>
  <% end %>
  <section class="item_list">
  <ol class="items list-group">
    <%= render @items %>
  </ol>
  <%= will_paginate @items %>
  </section>
<% else %>
<div class="center jumbotron">
  <h1>Welcome to the Government Application App</h1>

  <h2>
    This tool will hopefully make the tedious task of generating your cover letter for multiple competition a breeze.
  </h2>

  <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
</div>

<% end %>